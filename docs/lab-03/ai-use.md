# Lab 3 AI Use and Reflection

## Assistant used

ChatGPT, GPT-5.6 Sol, with repository, GitHub, and course-file tools available in the project environment.

## Selected key prompts

The course record calls for 6–10 selected prompts. The entries below are faithful concise records of actual AI-assisted Lab 3 work completed so far; they are not claimed to be verbatim transcripts. Additional entries are added only after the corresponding work actually occurs.

| # | Prompt name | Actual prompt text or faithful concise copy | Purpose / result | Short reflection |
|---:|---|---|---|---|
| 1 | Lab 3 engineering contract and Issue #41 preparation | “เริ่มเฉพาะงานเตรียมและ Engineering Contract ของ Lab 3 ใน Issue #41 โดยยึด `Lab_3_sheet.pdf` และข้อกำหนดอาจารย์เป็นหลัก ... ตรวจ repository เดิม, `AGENTS.md` ถ้ามี, Git status และ GitHub ล่าสุด ... แก้ความขัดแย้งระหว่างไฟล์แผนกับ Issues #41–#52 ... จัดทำเอกสารใน `docs/lab-03/` ... รอบนี้ยังไม่ implement ฟีเจอร์ ไม่ใช้ migration เปลี่ยนฐานข้อมูล ไม่ reset ข้อมูล และไม่ merge PR.” | Inspected the current repository/Git/GitHub state, created the Lab 3 staging and Issue #41 feature branches, reconciled the Lab 3 issue plan with the handout, drafted the Sprint 3 contract/traceability documents, and opened the docs-only PR #53 for real peer review. No Lab 3 product feature or database migration was implemented by this prompt. | Contract-first work exposed two sequencing risks before code was written: new status data must be readable when introduced, and authentication must be activated across backend identity and frontend transport together rather than leaving a mixed-version application. |
| 2 | Issue #43 migration implementation from handoff | “ทำงานตามไฟล์handoffเลยขอโหดๆ” followed by “ลุย” after the proposed #43 design was reviewed. | Implemented the Issue #43 forward schema/workflow migration, Argon2id password primitives, repeat-safe Lab 3 seed and local migrated-Requester provisioning, temporary selector role filtering, and minimum eight-status server/client compatibility. Verification covered populated migration/data preservation, canonical-email collision abort, seed reruns, password behavior, server/client suites, E2E, and responsive checks. | The highest-risk part was not adding columns; it was proving that Lab 2 data remains the same logical data after migration. The migration test therefore starts from a populated historical schema and checks IDs/FKs/content plus Attachment bytes, while collision detection runs before product mutation. |
| 3 | PR #55 peer-review response | “https://github.com/Tanaboonnnnn/toktickit/pull/55 เพื่อนคอมเม้นแก้ของเรามาอะ” followed by “งั้นจัดการให้เรียบร้อยเลย” | Verified the four Changes Requested items against the live branch before editing, added native session-type alignment, explicit migration transaction/late-failure rollback coverage, stronger Ticket/Attachment preservation evidence, and Lab 3 provisioning documentation. Rehearsed recovery evidence was then used to restore/reapply the corrected local migration so the database checksum/history matched the revised migration file before rerunning the full suite. | Review feedback was treated as a technical claim to verify, not a checklist to accept blindly. The transaction concern was first tested on the real `prisma migrate deploy` path, then made explicit in the migration file for review clarity; changing an already-applied local migration also required reconciling local migration history rather than hiding a checksum mismatch. |
| 4 | Issue #44 authentication foundation from handoff | “อ่านไฟล์handoffและดำเนินการทำงานissue44ให้เสร็จเรียบร้อยครบถ้วน และลองตรวจสอบไฟล์lab3เทียบกับโปรเจคเรารวมถึงแผนที่วางไว้เป็นไฟล์ที่แนบไปด้วย” followed by “ลงมือทำงานเลยเชื่อมต่อแล้ว” and “ทำต่อ” | Re-verified the merged #43 staging baseline, created the isolated `feature/44-lab3-auth-foundation` worktree, then implemented the backend authentication/session/password-change/authorization foundation with PostgreSQL-backed sessions, exact Origin/CSRF controls, bounded login throttling, safe current-user projection, stale-session revocation, and direct backend authorization tests. The retained Requester browser/header path was deliberately left pre-activation for #45, matching the reviewed cutover contract. | Engineering note (AI-assisted): keeping #44 as a foundation increment required separating “auth endpoints now exist” from “the application has switched identity.” The most important safety checks were server-derived identity, no secret-bearing DTOs, revocation against current account state, and fail-closed runtime configuration without creating a temporary production impersonation bypass. |
| 5 | Issue #45 cross-layer authentication activation from the latest checkpoint | “ทำต่อ Issue 45 จาก checkpoint ล่าสุด” | Continued the accepted #44 staging baseline into the coordinated #45 cutover: protected Requester APIs now derive identity from the authenticated Actor, the Development Requester selector/header/browser requester-ID authority was retired, Login/mandatory Change Password/authenticated shell/Logout were activated, current Requester screens were switched to cookie+CSRF transport, and a real-browser E2E-01 journey was added. Focused verification passed on code candidate `860f0af` without claiming the Issue #46 exhaustive Ticket/Attachment regression complete. | The main implementation lesson was that an authentication cutover is an integration boundary, not only a Login page. Backend identity, CSRF/multipart ordering, frontend session state, direct routes, logout behavior, and test fixtures had to change coherently. Legacy tests that still encode the Lab 2 impersonation setup must be evolved in their mapped issue rather than keeping a production bypass for compatibility. |
| 6 | Issue #46 authenticated Requester/Attachment continuity handoff | “อ่านไฟล์handoff…ตรวจสอบไฟล์ Lab 3 เทียบกับโปรเจค… @GitHub @ComGu Core” with the attached checkpoint directing the next work to Issue #46 | Re-verified live GitHub before coding: PR #57 was Approved and merged, Issue #45 was already closed, `lab3-staging` still pointed to `d443741`, and live Issue #46 required `feature/46-lab3-requester-regression`. A fresh isolated worktree was created from that exact merge and the #45 reviewer gate was reproduced successfully before #46 implementation. | The handoff text and the user's shorthand issue number conflicted, so source precedence mattered. Using the live Issue/merged contract prevented accidentally redoing #45 or branching from a stale local staging checkout. The fresh-worktree `.env` omission also demonstrated why an environment/setup failure must not be misreported as a product regression. |
| 7 | Issue #48 Staff Ticket Operations implementation | “อ่านไฟล์handoffและดำเนินการทำงานissue48ให้เสร็จเรียบร้อยครบถ้วน…” followed by approval to continue end-to-end and “ทำต่อเลย แล้วก็เปิดprไว้ให้เพื่อนฉันมารีวิวด้วยนะ” | Implemented the versioned Staff Ticket operations increment on the accepted #47 staging baseline: pure 64-pair workflow policy, PostgreSQL row-lock/optimistic-version mutation service, Claim/reassign/unassign within contract state rules, independent IT Priority, formal status transitions, Staff Detail controls, conflict reload/no auto-reapply, concurrency tests, and real-browser workflow/responsive coverage. | The concurrency boundary required both optimistic version checks and database row locking: versioning makes stale browser intent explicit while row locks serialize competing claims and coordinate owner eligibility with account-state changes. A browser failure also exposed a test-helper navigation race, which was debugged as test isolation rather than “fixed” in product routing. |
| 8 | PR #60 peer-review response | “https://github.com/Tanaboonnnnn/toktickit/pull/60 เพื่อนรีวิวมาแล้วแต่ว่ารีบแก้เล้ย” | Verified the Changes Requested feedback against the live PR and local branch, reproduced both owner-unaware Staff Detail status actions and the three EOF `git diff --check` findings, then added owner-aware UI regression coverage for unassigned NEW/CLOSED plus post-Claim enablement, cleaned the EOF issues, and reran focused and full verification before pushing the review fix. | Review feedback exposed a UI/backend contract mismatch rather than a backend workflow defect: the server correctly rejected ownerless non-Cancelled transitions, so the UI was tightened to expose only actions currently permitted by authoritative Ticket state while retaining backend enforcement as the security boundary. |
| 9 | Issue #50 Administrator User Management from current handoff | “อ่านไฟล์handoffและดำเนินการทำงานissue50ให้เสร็จเรียบร้อยครบถ้วน ... ให้ live GitHub + `origin/lab3-staging` + `docs/lab-03/*` มี authority สูงกว่า plan/audit เก่า” followed by “ทำงานต่อให้เสร็จจนถึงขั้นเปิดprรอเพื่อนมารีวิว” and “ทำต่อให้เสร็จเรียบร้อยอย่าหยุดกลางทาง” | Re-verified live Issue #50, accepted staging SHA, the reviewed FR-19..FR-22 / BR-30..BR-35 / AC-22..AC-26 contract and the Lab 3 handout, then implemented the minimalist Admin User Management API/UI in a fresh Issue #50 worktree. TDD covered safe list/search/filter, create/edit, canonical duplicate concurrency, separate initial-password reset/session revocation, self/last-admin/assigned-owner safety, assignment/account races, React states, and a real Chromium Administrator journey. | The highest-risk part was atomic account safety rather than form CRUD. User changes must coordinate with Ticket assignment and the last-active-Administrator invariant, while authVersion/version changes make stale access and stale browser intent fail explicitly. The feature stays deliberately small: no deletion, bulk operations, email reset delivery, multi-role or advanced identity scope. |
| 10 | Issue #51 integrated verification from handoff | “อ่านไฟล์handoffและดำเนินการทำงานissue51ให้เสร็จเรียบร้อยครบถ้วน ... ตรวจสอบไฟล์ lab3 เทียบกับโปรเจคเรารวมถึงแผนที่วางไว้ ... @GitHub @ComGu Core” followed by “ทำงานต่อ” | Reconciled live Issue #51, merged PR #62 staging, the reviewed Lab 3 contract and handoff, then added the remaining SAFE-01/STYLE-01/A11Y-01/SEC-01/RESP-01..03/TRACE-01 executable evidence. Fresh integrated verification includes direct backend security/privacy/CSRF/session checks, safe unexpected-error tests, traceability validation, and real-browser desktop/tablet/mobile journeys with Lab 3 screenshots. | Verification found no new product-code defect. It did reproduce one verification-helper bug: hidden desktop-table descendants were being measured as mobile touch targets. The helper was corrected to evaluate only rendered elements, then the unchanged product UI passed. This kept Issue #51 as an integration/release-evidence increment rather than inventing feature work. |

## Current reflection

Issues #41-#51 have now passed their recorded human-review/merge gates. Most recently, PR #63 was approved by `@L0u1sss` against reviewed head `84f2f321d9a84061dc9776d815de1a56ffa89f50` and merged to `lab3-staging` as `890e136e30cb56d87290ff3b70390e0ae26c3ce4`. Issue #52 starts from that exact accepted staging state and owns release reconciliation, SHA-labelled Playwright evidence, reviewed release-to-main workflow, and fresh final-main proof.

AI has been used for repository inspection, planning reconciliation, contract drafting, traceability design, migration/test implementation, and consistency checking. The output remains subject to student ownership: compare the documents and code against the Lab 3 handout, review the design decisions, and record only real implementation, test, migration, build, and peer-review results. The final submission's personal “My Reflection” remains the student's own reflection and is not fabricated here.

## Human ownership and decisions

The reviewed `docs/lab-03` contract is intended to become the Lab 3 source of truth after real peer review. Human/project decisions recorded so far are:

- TokTickIT Lab 3 continues the existing Lab 2 repository/product; historical Lab 2 evidence and applied migrations remain historical, while current source/tests/seed/configuration may evolve in new Lab 3 commits.
- `Lab_3_sheet.pdf` is the primary stakeholder/product-owner source for Sprint 3. Revision 2 and the readiness audit are supporting planning records, not replacements for the handout.
- New Ticket statuses must not be exposed before the integrated runtime can read and display them correctly.
- Authentication is split into a backend foundation increment followed by one cross-layer activation increment; after activation there is no Development Requester selector/header/body identity authority or test-only impersonation fallback in the normal application.
- The authorization matrix, exact Ticket transition edges, session/password constants, and optimistic-concurrency rules are documented as project decisions where the handout intentionally leaves design choices open.
- Only tests, migrations, reviews, approvals, and merges that actually occur may be promoted from planned/pending language to completed evidence.

AI assistance does not own these engineering decisions, and this record does not claim that a human manually authored text or code generated with AI assistance.

## Issue #49 AI-assisted implementation record

- **User request:** continue the handoff through the next Lab 3 work item, compare it with the Lab 3 handout/approved plan, finish the implementation, open a PR, and leave it awaiting peer review.
- **Source-of-truth reconciliation:** live GitHub showed Issue #48 already completed and PR #60 merged; the handoff and repository contract identified Issue #49 as the next open task, so accepted #48 work was not rewritten.
- **Implementation assistance:** AI helped inspect the reviewed FR/BR/AC/API/UI/Test DD, write RED tests first, implement the communication service/routes/client components, evolve the Requester Ticket DTO, and add the Chromium privacy journey.
- **Debugging assistance:** failures were investigated before fixes. Stale Lab 2/Issue #47 mocks and assertions were updated to the current Lab 3 contract; runtime Ticket validation was not weakened. A focused Requester UI timing failure was stabilized by waiting for loaded options rather than adding arbitrary sleeps.
- **Scope decisions preserved:** Public Comments and Internal Notes remain append-only plain text; Internal Notes are Staff/Admin-only; Requester indication records only a timestamp and never formally resolves/closes; Reopen clears the current-cycle indication; no Actions Taken, edit/delete, email notification, or Admin User Management work is pulled into Issue #49.
- **Verification evidence:** focused server 8/8, UI-05 4/4, E2E-04 2/2; full server 261/261, full client 126/126, Chromium 31/31, and retained responsive 10/10 all passed before PR submission. Known retained jsdom/Playwright diagnostics are recorded in tests.md and are not represented as failures.
- **Human ownership:** peer approval is not claimed here. Issue #49 remains subject to real PR review; any reviewer findings must be reproduced and addressed before merge.

## Issue #50 AI-assisted implementation record

- **User request:** read the current Issue #50 handoff from the local machine, compare the reviewed Lab 3 contract and handout with the live repository, complete Issue #50, then push and open a PR for a real peer review without self-merging.
- **Source-of-truth reconciliation:** live GitHub confirmed Issue #50 was open and `origin/lab3-staging` was the accepted PR #61 merge `8216683ecc8f4d3322dbbca95ec21dfc03428f34`; the implementation therefore branched from that exact staging state instead of the older roadmap/audit.
- **TDD/implementation assistance:** focused RED tests were observed before the corresponding Admin routes/services existed. The implementation then added backend-authoritative Administrator capability checks, safe User DTOs, canonical create/edit/reset contracts, password hashing/session invalidation, optimistic account versions, self/last-admin/primary-owner safety, the Zen Green User Management screen, and a real E2E-05 browser journey.
- **Concurrency/safety assistance:** canonical duplicate creation relies on the database unique constraint; account changes serialize the target User and share the Ticket-owner eligibility discipline; last-active-Administrator changes use a transaction-scoped advisory lock so two concurrent demotions cannot both succeed. Reactivation increments authorization state rather than reviving an earlier authenticated session.
- **Verification evidence before review:** focused server USER tests **14/14**, UI-06 **5/5**, E2E-05 **2/2**, full server **49 files / 275 tests**, full client **23 files / 131 tests**, Chromium **33/33**, retained responsive **10/10**, both production builds, and `git diff --check` passed. Generated responsive screenshots were removed instead of committed as Issue #50 evidence.
- **Human ownership:** live GitHub later recorded a real **Approved** review by `@thananun-7203` on head `e09e2cdda491c891b58df5017f9ec0bc679fdf7e`; PR #62 then merged to `lab3-staging` as `b568ff87dabb7716355eea622c5980dba5268db7`, and Issue #50 closed. This updates the living evidence without rewriting the earlier pre-review record.

## Issue #51 AI-assisted integrated verification record

- **User request:** read the Issue #51 handoff, compare it with the Lab 3 handout/current project and plan, complete the work end-to-end, and use live GitHub plus the repository state as authority.
- **Source-of-truth reconciliation:** live GitHub confirmed Issue #51 is open and assigned, PR #62 is already approved/merged, and `origin/lab3-staging` is `b568ff87dabb7716355eea622c5980dba5268db7`. A fresh worktree/branch was created from that exact merge rather than modifying accepted Issue #50 work.
- **Verification assistance:** AI helped audit the remaining Planned Test DD rows, add direct SAFE-01/SEC-01 backend checks, STYLE-01/A11Y-01 client checks, desktop/tablet/mobile integrated Playwright journeys, deterministic Lab 3 screenshot evidence, and a TRACE-01 structural audit.
- **Debugging assistance:** the mobile touch-target assertion initially reported a hidden desktop-table “View ticket” button. Inspection showed the element inherited hidden layout from its ancestor but the helper only checked the element's own computed display/visibility. The shared helper now ignores elements with no rendered client rect; the product UI itself was not changed.
- **Verification evidence before review:** focused migration/seed/security/safe-error **16/16**, STYLE/A11Y **5/5**, Issue #51 Chromium **4/4**, full server **51 files / 285 tests**, full client **25 files / 136 tests**, full Chromium **37/37**, retained responsive **10/10**, TRACE-01 **50 Test IDs / 32 ACs**, both production builds, and `git diff --check` passed.
- **Human ownership:** live GitHub later recorded a real **Approved** review by `@L0u1sss` against Issue #51 head `84f2f321d9a84061dc9776d815de1a56ffa89f50`; PR #63 merged to `lab3-staging` as `890e136e30cb56d87290ff3b70390e0ae26c3ce4`. Final-main release evidence remains Issue #52.

## Issue #52 AI-assisted release-evidence record

- **User request:** read the current handoff, complete Issue #52 against the Lab 3 handout/current repository/plan using GitHub and ComGu Core, and ensure the screenshot evidence is complete. The student then clarified explicitly that the final PDF must be deferred and that Playwright screenshots must not be forgotten.
- **Source-of-truth reconciliation:** live GitHub confirmed Issue #52 is open, PR #63 is Approved/merged, `origin/lab3-staging` is `890e136e30cb56d87290ff3b70390e0ae26c3ce4`, and `main` still points to the Lab 2 delivery until a reviewed Lab 3 release PR is merged.
- **Fresh baseline assistance:** a new Issue #52 worktree was created from the exact accepted staging merge. The first aggregate run exposed a fresh-worktree setup prerequisite because Prisma Client had not yet been generated after `npm ci`; `prisma generate` fixed the environment without a product-code change. Fresh baseline checks then passed: server **51 files / 285 tests**, client **25 files / 136 tests**, TRACE-01 **50 Test IDs / 32 ACs**, Chromium **37/37**, and retained responsive **10/10**.
- **Evidence tooling assistance:** Issue #52 adds a SHA-labelled Playwright capture command that reruns the integrated desktop/tablet/mobile Lab 3 journeys, requires exactly 27 screenshots, and emits a manifest with the tested source SHA. This supports later release-candidate and exact-final-main provenance rather than recycling screenshots without source identity.
- **Human/release boundary:** no Issue #52 approval, staging merge, release-PR approval, main merge, review-given evidence, student My Reflection, or final PDF is claimed before those events occur. The student's explicit instruction defers PDF creation to a later task.
