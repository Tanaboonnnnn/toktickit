# Lab 3 Regression Map

Issue #41 establishes the disposition model. Issue #42 performs the exhaustive per-file/per-case inventory and executes the safe baseline. Historical Lab 2 test results remain historical; current Lab 3 claims require current execution.

Disposition values:

- **Retain** โ€” requirement remains materially unchanged; run/update only setup/types needed by Lab 3.
- **Evolve** โ€” same product capability remains but identity/status/role expectation changes.
- **Replace** โ€” Lab 2-only mechanism is intentionally removed and receives explicit Lab 3 replacement coverage.

| Lab 2 Test ID / family | Disposition | Lab 3 requirement | Planned owner / replacement |
|---|---|---|---|
| UT-01 Ticket validation | Retain | Summary/description/priority validation remains | #46 `requester-regression.api.test.ts` + existing unit test maintained |
| UT-02 Ticket idempotency | Retain/Evolve | Same replay/conflict plus no operational-state reset | #46 REQ-03 + maintained existing unit test |
| UT-03 Ticket Number | Retain | Backend unique official number remains | #46 maintained existing test |
| UT-04 Ticket query | Evolve | Same list contract plus eight statuses | #43 minimum status compatibility; #46 STATUS-01 |
| UT-05/UT-06 Attachment rules | Retain | Attachment rules unchanged | #46 ATT-01 + maintained existing tests |
| UT-07 Development Requester context | Replace | Real authentication/current-user/session replaces selector/sessionStorage | #45 UI-01/AuthShell/routes + E2E-01 |
| UT-08 safe error serializer | Retain/Evolve | Same no-leak principle plus auth/session cases | #51 SAFE-01 + maintained existing test |
| API-01 reference data + Development Requesters | Split | Category/Related System retained behind the post-#45 authenticated/password-change-complete application gate; Development Requester endpoint removed after auth activation | #45 protected reference-data gate + Development Requester route removal; #46 REQ-01 retained reference checks |
| API-02 Requester header errors | Replace | Missing/invalid auth + spoofing resistance | #44/#45 AZ-01; #46 REQ-02 |
| API-03..API-10 Ticket create/list/detail | Retain/Evolve | Same capability with session-derived Requester and eight statuses | #45 activation setup; #46 REQ-01..03/STATUS-01 |
| API-11..API-18 Attachment lifecycle | Retain/Evolve | Same lifecycle under auth/CSRF/resource checks | #46 ATT-01..03 |
| API-19 migration/seed | Evolve | Populated forward migration and Lab 3 repeat-safe seed/provisioning | #43 MIG/SEED tests |
| API-20 all-endpoint failures | Retain/Evolve | Safe failure across new auth/staff/admin APIs | #51 SAFE-01 |
| UI-01 Requester Selection | Replace | Login/current-user bootstrap | #45 `Login.test.tsx`, `AuthShell.test.tsx` |
| UI-02 Requester Switcher | Replace | Logout/login identity transition and stale-state clearing | #45 AuthShell/E2E-01; #46 UI-02 |
| UI-03..UI-05 Create Ticket | Retain/Evolve | Same form/idempotency/Attachment behavior with authenticated identity | #46 UI-02/E2E-02 plus maintained existing tests |
| UI-06..UI-07 My Tickets | Evolve | Same states/controls plus all statuses | #46 UI-02/E2E-02 |
| UI-08 Ticket Detail | Evolve | Authenticated ownership + Public Comments/indication later | #46 UI-02; #49 UI-05 |
| UI-09 Attachment Panel | Retain/Evolve | Same Requester lifecycle under authenticated transport | #46 UI-02/E2E-02 |
| UI-10 accessibility | Retain/Extend | Same conventions across all new screens | #51 A11Y-01 |
| STYLE-01..03 Zen Green/responsive states | Retain/Extend | Same design system across Lab 3 screens | #51 STYLE-01/A11Y-01/RESP-01..03 |
| STYLE-04 no Staff/Comments controls | Replace selectively | Lab 3 intentionally adds Public Comments and role-specific Staff UI; Requester still must not see Internal Notes/staff mutations | #49 privacy UI/API + #51 security matrix |
| RESP-01..03 | Retain/Extend | Same evidence viewports, now all major Lab 3 screens | #51 RESP-01..03 |
| E2E-01 Requester selector/create/switch | Replace/Evolve | Real login -> Requester create/list/isolation | #45 E2E-01 + #46 E2E-02 |
| E2E-02..E2E-06 Requester/Ticket/Attachment | Retain/Evolve | Same user value through authenticated identity | #46 E2E-02 plus maintained relevant specs |
| E2E-07 accessibility/failure | Retain/Extend | Same quality/security expectations across Lab 3 | #51 A11Y/SAFE/security-boundaries |

## Rules for Updating Existing Tests

1. Do not keep `X-Development-Requester-Id` or a selector test bypass after #45 just to keep an old test green.
2. Do not delete still-valid validation, idempotency, ownership, Attachment, safe-error, responsive, or accessibility assertions.
3. A changed expectation must cite the changed Lab 3 FR/BR/AC or a proven test defect.
4. Tests under `lab-02` may be edited/moved when they are part of the current product suite; Git history preserves their historical Lab 2 version.
5. `docs/lab-02/` and submitted Lab 2 evidence are not rewritten to pretend the new behavior existed earlier.
6. #42 completes exact file/case/command mapping before feature implementation relies on this map.
## Exhaustive Issue #42 Per-File / Per-Case Inventory

This inventory was generated from the maintained Lab 1/Lab 2 test files on the Issue #42 base and then reviewed against the Lab 3 contract. `REPLACE` means the Lab 2-only mechanism remains executable **for the current pre-auth application** until its owning authentication issue lands; it does not authorize a future production bypass. `EVOLVE` keeps the user value/quality invariant while changing setup or expectations only in the named owning issue. Current execution evidence is recorded in `tests.md`, not inferred from this table.

| Original file | Lab 2 ID/family | Disposition | Reason | Lab 3 requirement | Current/replacement Test ID | Owner | Existing cases inventoried |
|---|---|---|---|---|---|---|---|
| `server/tests/lab-01/categories.test.ts` | LAB1 | RETAIN | Requirement remains valid; keep this case in the current suite and evolve only setup/types when an owning Lab 3 issue requires it. | Retained Lab 2 behavior / AC-31 regression | LAB1 | #42 current discovery; later owning feature only if contract changes | returns the four seeded categories in id order |
| `server/tests/lab-01/health.test.ts` | LAB1 | RETAIN | Requirement remains valid; keep this case in the current suite and evolve only setup/types when an owning Lab 3 issue requires it. | Retained Lab 2 behavior / AC-31 regression | LAB1 | #42 current discovery; later owning feature only if contract changes | returns 200 with status ok and the service name |
| `server/tests/lab-02/all-endpoints-failure.api.test.ts` | API-20 | EVOLVE | Quality invariant remains and extends across new authenticated/staff/admin surfaces without weakening the existing assertion. | FR-24; AC-29, AC-30 | SAFE-01 / A11Y-01 / STYLE-01 / RESP-01..03 | #51 | serializes a reference-data failure without internals<br>serializes a My Tickets failure without internals<br>serializes a Ticket Detail failure without internals<br>serializes Attachment metadata failure without internals<br>serializes Attachment removal failure without internals |
| `server/tests/lab-02/attachment-rules.unit.test.ts` | UT-06 | EVOLVE | Attachment rules/user value remain; transport and ownership setup evolves to authenticated identity/CSRF without preserving the legacy header as authority. | BR-38, BR-39; AC-09, AC-11 | ATT-01 / ATT-02 / ATT-03 / UI-02 / E2E-02 | #46 | counts only active rows and enforces the five-active boundary<br>trims valid reasons and enforces 3..200 characters |
| `server/tests/lab-02/attachment-storage.unit.test.ts` | UT-05/06 support | EVOLVE | Attachment rules/user value remain; transport and ownership setup evolves to authenticated identity/CSRF without preserving the legacy header as authority. | BR-38, BR-39; AC-09, AC-11 | ATT-01 / ATT-02 / ATT-03 / UI-02 / E2E-02 | #46 | cleans a staging directory when the payload write fails<br>treats rename as successful when staging-directory cleanup fails |
| `server/tests/lab-02/attachment-validation.unit.test.ts` | UT-05 | EVOLVE | Attachment rules/user value remain; transport and ownership setup evolves to authenticated identity/CSRF without preserving the legacy header as authority. | BR-38, BR-39; AC-09, AC-11 | ATT-01 / ATT-02 / ATT-03 / UI-02 / E2E-02 | #46 | rejects extension and MIME mismatch<br>rejects a permitted-looking file with the wrong signature<br>rejects unsupported extension/type and empty data<br>accepts exact inclusive boundary and rejects above it |
| `server/tests/lab-02/attachments-download.api.test.ts` | API-16 | EVOLVE | Attachment rules/user value remain; transport and ownership setup evolves to authenticated identity/CSRF without preserving the legacy header as authority. | BR-38, BR-39; AC-09, AC-11 | ATT-01 / ATT-02 / ATT-03 / UI-02 / E2E-02 | #46 | returns exact bytes and safe headers without touching Ticket updatedAt<br>uses the same 404 for missing, removed, mismatched, and cross-owner resources<br>returns safe 500 when active metadata points to missing bytes |
| `server/tests/lab-02/attachments-limit.api.test.ts` | API-14 | EVOLVE | Attachment rules/user value remain; transport and ownership setup evolves to authenticated identity/CSRF without preserving the legacy header as authority. | BR-38, BR-39; AC-09, AC-11 | ATT-01 / ATT-02 / ATT-03 / UI-02 / E2E-02 | #46 | allows five, rejects sixth, excludes removed, and allows replacement<br>serializes two concurrent uploads at four active rows |
| `server/tests/lab-02/attachments-metadata.api.test.ts` | API-15 | EVOLVE | Attachment rules/user value remain; transport and ownership setup evolves to authenticated identity/CSRF without preserving the legacy header as authority. | BR-38, BR-39; AC-09, AC-11 | ATT-01 / ATT-02 / ATT-03 / UI-02 / E2E-02 | #46 | returns active and removed metadata in createdAt/id order without internals |
| `server/tests/lab-02/attachments-ownership-failure.api.test.ts` | API-18 | EVOLVE | Attachment rules/user value remain; transport and ownership setup evolves to authenticated identity/CSRF without preserving the legacy header as authority. | BR-38, BR-39; AC-09, AC-11 | ATT-01 / ATT-02 / ATT-03 / UI-02 / E2E-02 | #46 | denies foreign metadata, upload, download, and removal<br>maps controlled storage failure to safe 500 and cleans final/staging entries<br>compensates when the database transaction fails after final storage |
| `server/tests/lab-02/attachments-remove.api.test.ts` | API-17 | EVOLVE | Attachment rules/user value remain; transport and ownership setup evolves to authenticated identity/CSRF without preserving the legacy header as authority. | BR-38, BR-39; AC-09, AC-11 | ATT-01 / ATT-02 / ATT-03 / UI-02 / E2E-02 | #46 | trims a valid reason, advances Ticket, denies later download, and keeps repeated removal non-disclosing |
| `server/tests/lab-02/attachments-upload-size.api.test.ts` | API-13 | EVOLVE | Attachment rules/user value remain; transport and ownership setup evolves to authenticated identity/CSRF without preserving the legacy header as authority. | BR-38, BR-39; AC-09, AC-11 | ATT-01 / ATT-02 / ATT-03 / UI-02 / E2E-02 | #46 | rejects an empty file<br>accepts exactly 5,242,880 bytes with a valid PDF signature<br>rejects 5,242,881 bytes with 413 and no metadata |
| `server/tests/lab-02/attachments-upload-type.api.test.ts` | API-12 | EVOLVE | Attachment rules/user value remain; transport and ownership setup evolves to authenticated identity/CSRF without preserving the legacy header as authority. | BR-38, BR-39; AC-09, AC-11 | ATT-01 / ATT-02 / ATT-03 / UI-02 / E2E-02 | #46 | leaves no metadata after type failures |
| `server/tests/lab-02/attachments-upload.api.test.ts` | API-11 | EVOLVE | Attachment rules/user value remain; transport and ownership setup evolves to authenticated identity/CSRF without preserving the legacy header as authority. | BR-38, BR-39; AC-09, AC-11 | ATT-01 / ATT-02 / ATT-03 / UI-02 / E2E-02 | #46 | parameterized cases accept `photo.JPG`/JPEG, PNG, WEBP, and PDF; each stores private generated bytes, hides storage internals, and advances the parent Ticket timestamp |
| `server/tests/lab-02/database-migration-seed.integration.test.ts` | API-19 | EVOLVE | Lab 2 migration/seed evidence remains historical; current migration verification evolves to populated forward migration and repeat-safe Lab 3 provisioning. | FR-23; AC-27, AC-28 | MIG-01 / MIG-02 / SEED-01 / SEED-02 | #43 | deploys the complete additive Lab 2 schema foundation<br>seeds the required reference data idempotently without deleting unrelated rows |
| `server/tests/lab-02/error-response.unit.test.ts` | UT-08 | EVOLVE | Quality invariant remains and extends across new authenticated/staff/admin surfaces without weakening the existing assertion. | FR-24; AC-29, AC-30 | SAFE-01 / A11Y-01 / STYLE-01 / RESP-01..03 | #51 | serializes documented ApiError fields without changing the safe envelope<br>maps unexpected errors to a generic 500 without exposing implementation details<br>keeps explicit duplicate conflicts safe and structured |
| `server/tests/lab-02/reference-data.api.test.ts` | API-01 | EVOLVE | Core user value remains, but authentication, eight-status handling, or role-specific Ticket Detail expectations change in Lab 3. | FR-05..FR-10; AC-08..AC-12 | REQ-01 / STATUS-01 / UI-02 | #43 minimum status gate; #45/#46; #49 where collaboration applies | returns active Categories only as id/name pairs ordered by ascending id<br>returns active Related Systems only as id/name pairs ordered by name then id<br>returns active Development Requesters only as id/name/email records ordered by name then id<br>returns the exact safe Category failure envelope without leaking internal details<br>returns valid empty arrays when reference queries find no active rows<br>returns the exact safe Related System failure envelope<br>returns the exact safe Development Requester failure envelope |
| `server/tests/lab-02/requester-context.api.test.ts` | API-02 | REPLACE | Legacy X-Development-Requester-Id identity errors are replaced by authenticated-session and spoofing-denial coverage. | BR-03; AC-03, AC-09 | AZ-01 / REQ-02 | #44/#46 | rejects a missing requester context with the safe 400 envelope<br>returns the same non-disclosing 404 for an unknown requester<br>returns the same non-disclosing 404 for an inactive requester<br>allows an active requester context to reach Ticket request validation |
| `server/tests/lab-02/ticket-detail.api.test.ts` | API-10 | EVOLVE | Core user value remains, but authentication, eight-status handling, or role-specific Ticket Detail expectations change in Lab 3. | FR-05..FR-10; AC-08..AC-12 | REQ-01 / STATUS-01 / UI-02 | #43 minimum status gate; #45/#46; #49 where collaboration applies | returns the owned Ticket and active/removed Attachment metadata in deterministic order<br>uses the same non-disclosing 404 for missing and foreign-owned Tickets<br>returns a safe 500 when Ticket-detail persistence fails |
| `server/tests/lab-02/ticket-idempotency.unit.test.ts` | UT-02 | EVOLVE | Ticket creation/idempotency/ownership user value remains, but requester identity becomes server-authenticated and operational state must not reset. | BR-03, BR-20, BR-21; AC-08..AC-10 | REQ-01 / REQ-02 / REQ-03 / UI-02 / E2E-02 | #45/#46 | treats the same requester and normalized logical content as replay-compatible<br>does not use clientRequestId itself as a content match |
| `server/tests/lab-02/ticket-number.unit.test.ts` | UT-03 | RETAIN | Requirement remains valid; keep this case in the current suite and evolve only setup/types when an owning Lab 3 issue requires it. | Retained Lab 2 behavior / AC-31 regression | UT-03 | #42 current discovery; later owning feature only if contract changes | uses the backend UTC date and the required uppercase alphanumeric format<br>retries a Ticket Number collision with fresh candidates and succeeds<br>stops after five candidates and exposes only a safe internal error |
| `server/tests/lab-02/ticket-query.unit.test.ts` | UT-04 | EVOLVE | Core user value remains, but authentication, eight-status handling, or role-specific Ticket Detail expectations change in Lab 3. | FR-05..FR-10; AC-08..AC-12 | REQ-01 / STATUS-01 / UI-02 | #43 minimum status gate; #45/#46; #49 where collaboration applies | applies documented defaults and deterministic id desc tie ordering<br>trims search and treats empty-after-trim as unrestricted<br>accepts the exact search maximum and rejects values above it<br>validates category, enum filters, and sort allowlists<br>validates page and permitted page sizes<br>rejects unknown and repeated scalar query parameters |
| `server/tests/lab-02/ticket-validation.unit.test.ts` | UT-01 | RETAIN | Requirement remains valid; keep this case in the current suite and evolve only setup/types when an owning Lab 3 issue requires it. | Retained Lab 2 behavior / AC-31 regression | UT-01 | #42 current discovery; later owning feature only if contract changes | accepts inclusive Summary and Description boundaries and trims both values<br>rejects invalid priorities and non-positive/non-integer reference IDs<br>requires a valid UUID and all create fields<br>accepts a valid UUID without imposing a version-specific restriction |
| `server/tests/lab-02/tickets-create-validation.api.test.ts` | API-04 | EVOLVE | Ticket creation/idempotency/ownership user value remains, but requester identity becomes server-authenticated and operational state must not reset. | BR-03, BR-20, BR-21; AC-08..AC-10 | REQ-01 / REQ-02 / REQ-03 / UI-02 / E2E-02 | #45/#46 | rejects malformed JSON with the safe validation envelope<br>returns named 400 field errors and creates no Ticket for invalid input |
| `server/tests/lab-02/tickets-create.api.test.ts` | API-03 | EVOLVE | Ticket creation/idempotency/ownership user value remains, but requester identity becomes server-authenticated and operational state must not reset. | BR-03, BR-20, BR-21; AC-08..AC-10 | REQ-01 / REQ-02 / REQ-03 / UI-02 / E2E-02 | #45/#46 | creates exactly one Ticket with backend-controlled values and the documented shape |
| `server/tests/lab-02/tickets-failure.api.test.ts` | API-06 | EVOLVE | Ticket creation/idempotency/ownership user value remains, but requester identity becomes server-authenticated and operational state must not reset. | BR-03, BR-20, BR-21; AC-08..AC-10 | REQ-01 / REQ-02 / REQ-03 / UI-02 / E2E-02 | #45/#46 | returns a safe 500, commits no Ticket, and permits retry with the same logical ID<br>retries a real Prisma Ticket Number uniqueness collision with a fresh candidate |
| `server/tests/lab-02/tickets-idempotency.api.test.ts` | API-05 | EVOLVE | Ticket creation/idempotency/ownership user value remains, but requester identity becomes server-authenticated and operational state must not reset. | BR-03, BR-20, BR-21; AC-08..AC-10 | REQ-01 / REQ-02 / REQ-03 / UI-02 / E2E-02 | #45/#46 | replays the same normalized request without touching updatedAt, even after references deactivate<br>returns 409 and creates nothing for conflicting payload or requester reuse<br>resolves two concurrent identical creates to one Ticket and one replay |
| `server/tests/lab-02/tickets-ownership.api.test.ts` | API-07 | EVOLVE | Ticket creation/idempotency/ownership user value remains, but requester identity becomes server-authenticated and operational state must not reset. | BR-03, BR-20, BR-21; AC-08..AC-10 | REQ-01 / REQ-02 / REQ-03 / UI-02 / E2E-02 | #45/#46 | enforces requester ownership in the database query and exposes only TicketListItem fields |
| `server/tests/lab-02/tickets-pagination.api.test.ts` | API-09 | EVOLVE | Core user value remains, but authentication, eight-status handling, or role-specific Ticket Detail expectations change in Lab 3. | FR-05..FR-10; AC-08..AC-12 | REQ-01 / STATUS-01 / UI-02 | #43 minimum status gate; #45/#46; #49 where collaboration applies | uses updatedAt desc by default and id desc for equal primary values<br>returns valid pages, sizes, totals, and an empty positive out-of-range page<br>rejects repeated scalar query parameters |
| `server/tests/lab-02/tickets-query.api.test.ts` | API-08 | EVOLVE | Core user value remains, but authentication, eight-status handling, or role-specific Ticket Detail expectations change in Lab 3. | FR-05..FR-10; AC-08..AC-12 | REQ-01 / STATUS-01 / UI-02 | #43 minimum status gate; #45/#46; #49 where collaboration applies | searches Ticket Number or Summary case-insensitively with substring and trimming<br>combines Category, Requested Priority, and Current Status filters with AND<br>rejects an overlong search safely |
| `client/tests/lab-02/accessibility.test.tsx` | UI-10 | EVOLVE | Quality invariant remains and extends across new authenticated/staff/admin surfaces without weakening the existing assertion. | FR-24; AC-29, AC-30 | SAFE-01 / A11Y-01 / STYLE-01 / RESP-01..03 | #51 | gives core Create Ticket controls visible names, required semantics, and associated validation<br>supports keyboard activation while retaining native disabled semantics<br>names list actions and communicates loading/busy/pagination state<br>identifies Attachment actions and keeps removed state understandable without color |
| `client/tests/lab-02/AttachmentPanel.test.tsx` | UI-09 | EVOLVE | Attachment rules/user value remain; transport and ownership setup evolves to authenticated identity/CSRF without preserving the legacy header as authority. | BR-38, BR-39; AC-09, AC-11 | ATT-01 / ATT-02 / ATT-03 / UI-02 / E2E-02 | #46 | supports selected and uploading states, then refreshes after upload<br>executes a definitive upload failure through the real Upload action<br>rejects an invalid local Attachment before any upload request<br>shows the active five-file limit and removed metadata without actions<br>validates removal reason, supports cancel, and refreshes after successful removal<br>shows safe upload and download failures and preserves long filenames<br>gates an ambiguous Ticket Detail upload on successful reconciliation<br>offers only Retry status check when ambiguous reconciliation fails<br>revokes the temporary object URL after a successful download<br>uses the post-mutation Ticket response as authoritative state and re-enables capacity after removal |
| `client/tests/lab-02/AttachmentPreselection.test.tsx` | UI-04 | EVOLVE | Attachment rules/user value remain; transport and ownership setup evolves to authenticated identity/CSRF without preserving the legacy header as authority. | BR-38, BR-39; AC-09, AC-11 | ATT-01 / ATT-02 / ATT-03 / UI-02 / E2E-02 | #46 | renders the Attachment section stating allowed types, 5 MB limit, and five-file limit<br>selects a valid PNG file showing filename, readable size/type, Selected state, and Remove action<br>accepts a file at exactly 5,242,880 bytes inclusive boundary<br>enforces maximum five selected files keeping first five valid<br>removes a selected file from local selection<br>removes an invalid file from local selection<br>retains eligible selected files after definitive Ticket-create failure<br>uploads selected files after the Ticket is created |
| `client/tests/lab-02/CreateTicketForm.test.tsx` | UI-03 | EVOLVE | Ticket creation/idempotency/ownership user value remains, but requester identity becomes server-authenticated and operational state must not reset. | BR-03, BR-20, BR-21; AC-08..AC-10 | REQ-01 / REQ-02 / REQ-03 / UI-02 / E2E-02 | #45/#46 | renders required and read-only fields after selecting a Requester<br>loads Category and Related System options from the API without hard-coding values<br>shows reference-data loading state and blocks submission while unresolved<br>shows Category successful empty state with submission blocked<br>shows Related System successful empty state with submission blocked<br>shows safe independent Category failure with Retry preserving entered text<br>shows safe independent Related System failure with Retry preserving entered text<br>preserves successful reference list when only one resource fails<br>keeps submission blocked when either required list is empty or failed |
| `client/tests/lab-02/CreateTicketSubmission.test.tsx` | UI-05 | EVOLVE | Ticket creation/idempotency/ownership user value remains, but requester identity becomes server-authenticated and operational state must not reset. | BR-03, BR-20, BR-21; AC-08..AC-10 | REQ-01 / REQ-02 / REQ-03 / UI-02 / E2E-02 | #45/#46 | sends correct headers and body for first-create success<br>shows a clear next action after successful Ticket creation<br>shows busy state and prevents duplicate click during submission<br>handles 200 replay success as success with official ticket number<br>preserves form values after definitive HTTP failure<br>uses a new clientRequestId when data changes after definitive no-create failure<br>keeps same clientRequestId on ambiguous retry without creating a new logical request<br>freezes Ticket fields while the ambiguous outcome remains unresolved<br>treats an HTTP 500 as definitive no-create failure and retries the same logical request<br>offers View Ticket and My Tickets actions after successful creation<br>uploads each selected valid file after Ticket creation and keeps the official number visible<br>keeps Ticket success and sibling upload success when one attachment fails<br>marks an uncertain upload ambiguous, reloads metadata, and only retries on explicit action<br>keeps Retry upload hidden when the first status reconciliation fails |
| `client/tests/lab-02/CreateTicketValidation.test.tsx` | UI-04 | EVOLVE | Ticket creation/idempotency/ownership user value remains, but requester identity becomes server-authenticated and operational state must not reset. | BR-03, BR-20, BR-21; AC-08..AC-10 | REQ-01 / REQ-02 / REQ-03 / UI-02 / E2E-02 | #45/#46 | shows required Category validation without calling the API<br>shows required Related System validation without calling the API<br>accepts Summary at exact lower boundary of 5 trimmed chars<br>enforces maxLength=120 on Summary input<br>rejects Summary above the 120-character maximum when a programmatic change delivers more than maxLength allows<br>accepts Description at exact lower boundary of 10 trimmed chars<br>enforces maxLength=2000 on Description textarea<br>rejects Description above the 2000-character maximum when a programmatic change delivers more than maxLength allows<br>rejects missing Requested Priority without calling the API<br>moves focus to first invalid field when multiple fields are invalid<br>preserves entered values after validation failure |
| `client/tests/lab-02/MyTicketsControls.test.tsx` | UI-07 | EVOLVE | Core user value remains, but authentication, eight-status handling, or role-specific Ticket Detail expectations change in Lab 3. | FR-05..FR-10; AC-08..AC-12 | REQ-01 / STATUS-01 / UI-02 | #43 minimum status gate; #45/#46; #49 where collaboration applies | uses the documented initial defaults and applies search only on Search/Enter<br>sends Category, Priority, Status, sorting and page-size controls and resets page to one<br>supports pagination boundaries and Clear resetting all controls<br>recovers an out-of-range page with exactly one final-page request<br>sends the selected Requester context and preserves the applied query on Retry |
| `client/tests/lab-02/MyTicketsStates.test.tsx` | UI-06 | EVOLVE | Core user value remains, but authentication, eight-status handling, or role-specific Ticket Detail expectations change in Lab 3. | FR-05..FR-10; AC-08..AC-12 | REQ-01 / STATUS-01 / UI-02 | #43 minimum status gate; #45/#46; #49 where collaboration applies | communicates loading and does not show stale rows during a new query<br>shows Empty with a Create Ticket action when ownership has no Tickets<br>uses exactly one unrestricted page=1&pageSize=10 probe to distinguish No Results<br>shows safe Failure text and retries the current query<br>clears the old Requester rows before loading the new Requester list |
| `client/tests/lab-02/requester-context.unit.test.tsx` | UT-07 | REPLACE | Development Requester selector/sessionStorage/switching is a Lab 2-only identity mechanism and is removed at the authentication activation gate. | FR-01..FR-05; BR-03; AC-01..AC-06 | UI-01 / E2E-01 | #45 | loads active Development Requesters when the application context initializes<br>enters selection state without a current Requester when storage is missing<br>restores a syntactically valid stored ID only when it is in the active API result<br>clears a syntactically valid but unknown or inactive ID after active data loads<br>keeps a valid stored ID unresolved during API failure and supports safe retry<br>selects an active Requester and persists only its canonical integer ID<br>remounts the Requester-scoped boundary when switching Requesters |
| `client/tests/lab-02/RequesterSelection.test.tsx` | UI-01 | REPLACE | Development Requester selector/sessionStorage/switching is a Lab 2-only identity mechanism and is removed at the authentication activation gate. | FR-01..FR-05; BR-03; AC-01..AC-06 | UI-01 / E2E-01 | #45 | does not render the legacy Lab 1 Check System control in the Lab 2 flow<br>shows loading feedback while active Requesters are loading<br>renders active name/email options with an accessible disabled Continue action<br>establishes the selected Requester only after Continue<br>shows a distinct no-active-Requester state for an empty successful response<br>shows safe failure wording and retries through the existing context<br>clears malformed persisted context and returns to Requester Selection<br>clears an unknown or inactive persisted ID after active Requesters load<br>enters the shell directly when a valid active Requester is restored |
| `client/tests/lab-02/RequesterSwitcher.test.tsx` | UI-02 | REPLACE | Development Requester selector/sessionStorage/switching is a Lab 2-only identity mechanism and is removed at the authentication activation gate. | FR-01..FR-05; BR-03; AC-01..AC-06 | UI-01 / E2E-01 | #45 | returns to selection and replaces the shell context when switching Requesters<br>remounts requester-scoped client state when the Requester changes<br>keeps shell navigation understandable while exposing My Tickets<br>keeps Change Requester keyboard-operable and visibly focusable |
| `client/tests/lab-02/responsive-styles.test.tsx` | STYLE-03 | EVOLVE | Quality invariant remains and extends across new authenticated/staff/admin surfaces without weakening the existing assertion. | FR-24; AC-29, AC-30 | SAFE-01 / A11Y-01 / STYLE-01 / RESP-01..03 | #51 | allows long summaries, descriptions, validation messages, and filenames to wrap<br>keeps table overflow contained and defines mobile cards plus responsive control reflow |
| `client/tests/lab-02/state-indicators.test.tsx` | STYLE-02 | EVOLVE | Core user value remains, but authentication, eight-status handling, or role-specific Ticket Detail expectations change in Lab 3. | FR-05..FR-10; AC-08..AC-12 | REQ-01 / STATUS-01 / UI-02 | #43 minimum status gate; #45/#46; #49 where collaboration applies | renders textual success, priority, and status cues<br>renders safe error text and visible disabled/limit explanations<br>renders active and removed Attachment meaning with text and scoped actions<br>keeps partial success understandable when one Attachment fails |
| `client/tests/lab-02/ticket-detail-scope.test.tsx` | STYLE-04 | EVOLVE | Core user value remains, but authentication, eight-status handling, or role-specific Ticket Detail expectations change in Lab 3. | FR-05..FR-10; AC-08..AC-12 | REQ-01 / STATUS-01 / UI-02 | #43 minimum status gate; #45/#46; #49 where collaboration applies | renders no collaboration, IT Staff, ownership mutation, or lifecycle controls |
| `client/tests/lab-02/TicketDetail.test.tsx` | UI-08 | EVOLVE | Core user value remains, but authentication, eight-status handling, or role-specific Ticket Detail expectations change in Lab 3. | FR-05..FR-10; AC-08..AC-12 | REQ-01 / STATUS-01 / UI-02 | #43 minimum status gate; #45/#46; #49 where collaboration applies | announces loading and does not show stale detail data<br>renders the owned Ticket read-only fields and separate active/removed metadata<br>shows neutral unavailable wording for a documented 404<br>shows safe failure text and retries with the same Ticket ID and requester context<br>returns through Back to My Tickets<br>opens the selected Ticket from desktop and mobile My Tickets View actions<br>clears the previous detail before loading a newly selected Requester |
| `client/tests/lab-02/zen-green-styles.test.tsx` | STYLE-01 | EVOLVE | Quality invariant remains and extends across new authenticated/staff/admin surfaces without weakening the existing assertion. | FR-24; AC-29, AC-30 | SAFE-01 / A11Y-01 / STYLE-01 / RESP-01..03 | #51 | exposes the exact UI-spec color, focus, and elevation tokens<br>uses reusable state and hierarchy classes instead of inline color styling |
| `e2e/lab-02/accessibility-failure.spec.ts` | Mapped family | EVOLVE | Quality invariant remains and extends across new authenticated/staff/admin surfaces without weakening the existing assertion. | FR-24; AC-29, AC-30 | SAFE-01 / A11Y-01 / STYLE-01 / RESP-01..03 | #51 | E2E-07 supports keyboard focus, validation, and non-color cues<br>E2E-07 presents a safe 500 failure without server internals |
| `e2e/lab-02/attachment-lifecycle.spec.ts` | Mapped family | EVOLVE | Attachment rules/user value remain; transport and ownership setup evolves to authenticated identity/CSRF without preserving the legacy header as authority. | BR-38, BR-39; AC-09, AC-11 | ATT-01 / ATT-02 / ATT-03 / UI-02 / E2E-02 | #46 | E2E-06 covers valid upload, download, removal, and replacement<br>E2E-06 rejects invalid type and oversized files before upload<br>E2E-06 enforces five active files and re-enables selection after removal<br>E2E-06 preserves a created Ticket during deterministic partial success<br>E2E-06 reconciles an ambiguous upload before offering manual retry<br>E2E-06 denies cross-owner Attachment operations without leaking metadata |
| `e2e/lab-02/create-validation-retry.spec.ts` | Mapped family | RETAIN | Requirement remains valid; keep this case in the current suite and evolve only setup/types when an owning Lab 3 issue requires it. | Retained Lab 2 behavior / AC-31 regression | Mapped family | #42 current discovery; later owning feature only if contract changes | E2E-02 validates locally, retains values, and retries a safe backend failure |
| `e2e/lab-02/duplicate-submission.spec.ts` | Mapped family | RETAIN | Requirement remains valid; keep this case in the current suite and evolve only setup/types when an owning Lab 3 issue requires it. | Retained Lab 2 behavior / AC-31 regression | Mapped family | #42 current discovery; later owning feature only if contract changes | E2E-03 reconciles a lost Ticket-create response with the same request key |
| `e2e/lab-02/my-tickets.spec.ts` | Mapped family | RETAIN | Requirement remains valid; keep this case in the current suite and evolve only setup/types when an owning Lab 3 issue requires it. | Retained Lab 2 behavior / AC-31 regression | Mapped family | #42 current discovery; later owning feature only if contract changes | E2E-04 exercises My Tickets search, filters, sorting, pagination, and states |
| `e2e/lab-02/requester-ticket-journey.spec.ts` | Mapped family | REPLACE | Selector switching in this journey is superseded by real login/logout identity transitions while the create/isolation user value continues in authenticated regression. | AC-01, AC-06, AC-08..AC-12 | E2E-01 / E2E-02 | #45/#46 | E2E-01 creates and scopes a Ticket across Requester switching |
| `e2e/lab-02/responsive-desktop.spec.ts` | RESP-01 | EVOLVE | Quality invariant remains and extends across new authenticated/staff/admin surfaces without weakening the existing assertion. | FR-24; AC-29, AC-30 | SAFE-01 / A11Y-01 / STYLE-01 / RESP-01..03 | #51 | renders Development Requester Selection without clipping<br>keeps Create Ticket fields and actions usable<br>uses the desktop My Tickets table and supports an empty state<br>renders read-only Ticket Detail with Attachment panel |
| `e2e/lab-02/responsive-mobile.spec.ts` | RESP-03 | EVOLVE | Quality invariant remains and extends across new authenticated/staff/admin surfaces without weakening the existing assertion. | FR-24; AC-29, AC-30 | SAFE-01 / A11Y-01 / STYLE-01 / RESP-01..03 | #51 | stacks Create Ticket fields and focuses the first invalid field<br>uses readable ticket cards and a no-results state<br>wraps long removed Attachment metadata and hides unavailable actions |
| `e2e/lab-02/responsive-tablet.spec.ts` | RESP-02 | EVOLVE | Quality invariant remains and extends across new authenticated/staff/admin surfaces without weakening the existing assertion. | FR-24; AC-29, AC-30 | SAFE-01 / A11Y-01 / STYLE-01 / RESP-01..03 | #51 | reflows filters and keeps Summary/Description readable<br>captures a real partial-success Create Ticket state<br>keeps Ticket Detail and Attachment actions usable |
| `e2e/lab-02/ticket-detail-ownership.spec.ts` | Mapped family | EVOLVE | Core user value remains, but authentication, eight-status handling, or role-specific Ticket Detail expectations change in Lab 3. | FR-05..FR-10; AC-08..AC-12 | REQ-01 / STATUS-01 / UI-02 | #43 minimum status gate; #45/#46; #49 where collaboration applies | E2E-05 renders owned read-only detail and denies a foreign Ticket |
