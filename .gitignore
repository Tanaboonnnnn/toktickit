# dependencies
node_modules/
# env & secrets
.env
*.env
!.env.example
# build output
dist/
build/
# Playwright transient output (course screenshots under artifacts/ remain tracked)
playwright-report/
test-results/
# Issue #52 exact-head/final-main evidence is generated from an already-existing SHA.
# Keep it out of that same commit or the commit would invalidate its own provenance.
artifacts/lab-03/screenshots/issue-52/
# prisma
server/prisma/*.db
# private runtime attachment bytes
server/uploads/
# logs & OS
*.log
.DS_Store
Thumbs.db
Desktop.ini
